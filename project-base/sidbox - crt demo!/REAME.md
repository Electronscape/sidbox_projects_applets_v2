# this needs filling

This project is for building the SIDBOX music box mode, when NOT booting into the GUI CoderGirl + desktop (needs a name)

HERE is the runnable test code for building something that will eventually will be installed into rom for when "loadwb" is NOT found on the sdcard OR if there is no sdcard is in the machine.

so this is just a big button, file lister, music player UI, animations, that stuff xD
