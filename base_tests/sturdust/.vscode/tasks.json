{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile To ARM now :)",
      "type": "shell",
      //"command": "${workspaceFolder}/compile.bat",  // For when you're doing it in windows
      "command": "make",  // For linux
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}